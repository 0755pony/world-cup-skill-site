{
  "name": "green-soccer-skill-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "check": "node --check functions/api/predict.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
